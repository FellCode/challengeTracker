import { ConfirmationDialogModel } from './confirmation-dialog-model';

describe('ConfirmationDialogModel', () => {
  it('should create an instance', () => {
    expect(new ConfirmationDialogModel()).toBeTruthy();
  });
});
